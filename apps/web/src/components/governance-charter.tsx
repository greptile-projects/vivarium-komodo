"use client";
import {FormEvent,useCallback,useEffect,useState} from "react";
import {Badge,Button} from "@/components/ui";

type Standing={id:string;principal_id:string;role:string;state:string;responsibilities:string[];evidence:{kind:string;reference:string;summary:string}[];available_nominations:string[];available_appeals:string[];operational_authority:string[];term_ends_at?:string};
type Charter={active_version?:number;current:{version:number;state:string;title:string;purpose:string;roles:{name:string;purpose:string;eligibility:string[];responsibilities:string[];minimum_members:number;term_days?:number}[];decision_classes:{name:string;description:string;eligible_roles:string[];participation:string;quorum:number;threshold:number;protected_resources:string[]}[];participation_rules:string[];protected_resources:string[];procedures:{removal:string;succession:string;vacancy:string};amendment_policy:{eligible_roles:string[];notice_days:number;quorum:number;threshold:number};preview:{items:{kind:string;resource:string;state:string;detail:string;blocking:boolean}[];blockers:string[];authority_granted:boolean};author_id:string;change_reason:string;created_at:string};history:Charter["current"][];approvals:{actor_id:string;version:number;note?:string;created_at:string}[];exceptions:{id:string;version:number;scope:string;reason:string;expires_at:string;actor_id:string}[];standings:Standing[]};
const lines=(v:FormDataEntryValue|null)=>String(v||"").split("\n").map(x=>x.trim()).filter(Boolean);
export function GovernanceCharter({scope,id,canManage}:{scope:"repositories"|"organizations";id:string;canManage:boolean}){
 const base=`/${scope}/${id}/governance-charter`,[charter,setCharter]=useState<Charter>(),[error,setError]=useState("");
 const load=useCallback(async()=>{const r=await fetch(`/api${base}`);if(r.ok)setCharter(await r.json());else if(r.status!==404)setError("Governance charter could not be loaded.")},[base]);useEffect(()=>{const timer=setTimeout(()=>void load(),0);return()=>clearTimeout(timer)},[load]);
 async function mutate(path:string,body:unknown){setError("");const r=await fetch(`/api${base}${path}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});if(!r.ok){const v=await r.json().catch(()=>({}));setError(v.error==="charter_conflict"?"Activation is blocked by missing approval, stale version, or an impossible rule.":"The charter could not be updated.");return}setCharter(await r.json())}
 async function publish(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),role=String(f.get("role")),resource=lines(f.get("resources"));await mutate("",{expected_version:charter?.current.version||0,title:f.get("title"),purpose:f.get("purpose"),roles:[{name:role,purpose:f.get("role_purpose"),eligibility:lines(f.get("eligibility")),responsibilities:lines(f.get("responsibilities")),minimum_members:Number(f.get("minimum")),term_days:Number(f.get("term"))}],decision_classes:[{name:f.get("decision"),description:f.get("decision_description"),eligible_roles:[role],participation:f.get("participation"),quorum:Number(f.get("quorum")),threshold:Number(f.get("threshold")),protected_resources:resource}],participation_rules:lines(f.get("rules")),protected_resources:resource,procedures:{removal:f.get("removal"),succession:f.get("succession"),vacancy:f.get("vacancy")},amendment_policy:{eligible_roles:[role],notice_days:Number(f.get("notice")),quorum:Number(f.get("amendment_quorum")),threshold:Number(f.get("amendment_threshold"))},change_reason:f.get("reason")})}
 return <section className="investigation-workspace"><div className="section-heading"><div><span className="eyebrow">Decision rights</span><h2>Project governance</h2><p>Published rules explain who may decide what and how stewardship changes. They never replace repository, organization, branch, release, environment, security, or agent authority.</p></div>{charter&&<Badge>{charter.current.state} · v{charter.current.version}</Badge>}</div>{error&&<p className="form-error" role="alert">{error}</p>}
 {charter&&<div className="panel stack"><h3>{charter.current.title}</h3><p>{charter.current.purpose}</p><p><strong>Protected resources:</strong> {charter.current.protected_resources.join(" · ")}</p>{charter.current.roles.map(r=><div key={r.name}><h4>{r.name}</h4><p>{r.purpose} · minimum {r.minimum_members}{r.term_days?` · ${r.term_days}-day term`:""}</p><p><strong>Eligible:</strong> {r.eligibility.join(" · ")}<br/><strong>Responsible:</strong> {r.responsibilities.join(" · ")}</p></div>)}{charter.current.decision_classes.map(d=><div key={d.name}><h4>{d.name}</h4><p>{d.description} · {d.participation} · quorum {d.quorum} · threshold {d.threshold}%</p></div>)}<p><strong>Removal:</strong> {charter.current.procedures.removal}<br/><strong>Succession:</strong> {charter.current.procedures.succession}<br/><strong>Vacancy:</strong> {charter.current.procedures.vacancy}</p><p><strong>Amendments:</strong> {charter.current.amendment_policy.notice_days} days notice · quorum {charter.current.amendment_policy.quorum} · {charter.current.amendment_policy.threshold}% threshold</p><h4>Live authority preview</h4>{charter.current.preview.items.map((x,i)=><p className={x.blocking?"form-error":""} key={`${x.resource}:${i}`}><strong>{x.kind} · {x.state}:</strong> {x.detail}</p>)}<small>Authority granted by charter: no · authored by {charter.current.author_id}</small>{charter.approvals.filter(a=>a.version===charter.current.version).map(a=><p key={a.actor_id}>Approved by {a.actor_id}{a.note?` — ${a.note}`:""}</p>)}{charter.exceptions.map(x=><p key={x.id}>Exception to v{x.version}: {x.scope} — {x.reason} (expires {new Date(x.expires_at).toLocaleString()})</p>)}{canManage&&charter.current.state!=="active"&&<div className="button-row"><Button onClick={()=>mutate("/approvals",{version:charter.current.version,note:"Maintainer approval"})}>Approve revision</Button><Button onClick={()=>mutate("/activation",{version:charter.current.version})}>Activate after fresh preview</Button></div>}<details><summary>Immutable revision history</summary>{charter.history.map(x=><p key={x.version}>Version {x.version} · {x.state} · {x.change_reason} · {x.author_id}</p>)}</details></div>}
 {canManage&&<details className="panel"><summary>{charter?"Draft a new immutable revision":"Publish the first charter draft"}</summary><form className="stack" onSubmit={publish}><label>Charter title<input required name="title" defaultValue={charter?.current.title}/></label><label>Purpose<textarea required name="purpose" defaultValue={charter?.current.purpose}/></label><div className="content-grid"><label>Governance role<input required name="role" defaultValue="Maintainer"/></label><label>Minimum members<input required type="number" min="1" name="minimum" defaultValue="1"/></label><label>Term days<input required type="number" min="1" name="term" defaultValue="365"/></label></div><label>Role purpose<textarea required name="role_purpose"/></label><label>Eligibility, one per line<textarea required name="eligibility"/></label><label>Responsibilities, one per line<textarea required name="responsibilities"/></label><div className="content-grid"><label>Decision class<input required name="decision" defaultValue="Project policy"/></label><label>Participation<select name="participation"><option>open_deliberation</option><option>eligible_roles_only</option></select></label><label>Quorum<input required type="number" min="1" name="quorum" defaultValue="1"/></label><label>Threshold %<input required type="number" min="1" max="100" name="threshold" defaultValue="50"/></label></div><label>Decision description<textarea required name="decision_description"/></label><label>Protected resource classes, one per line<textarea required name="resources" placeholder={"ownership\nbranches:main\nreleases\nsecurity\nagents"}/></label><label>Participation rules<textarea required name="rules"/></label><label>Removal procedure<textarea required name="removal"/></label><label>Succession procedure<textarea required name="succession"/></label><label>Vacancy procedure<textarea required name="vacancy"/></label><div className="content-grid"><label>Amendment notice days<input required type="number" min="0" name="notice" defaultValue="7"/></label><label>Amendment quorum<input required type="number" min="1" name="amendment_quorum" defaultValue="1"/></label><label>Amendment threshold %<input required type="number" min="1" max="100" name="amendment_threshold" defaultValue="67"/></label></div><label>Change reason<input required name="reason"/></label><Button type="submit">Publish draft and preview conflicts</Button></form></details>}</section>
}
