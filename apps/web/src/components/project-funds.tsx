"use client";
import {FormEvent,useCallback,useEffect,useState} from "react";
import {Badge,Button} from "@/components/ui";
type Fund={id:string;version:number;terms:{name:string;description:string;steward_ids:string[];accepted_funding_sources:string[];unit:string;unit_kind:string;spending_limits:{per_allocation:number;per_recipient:number;total:number};approval_rule:{minimum_approvals:number;approver_ids:string[];threshold:number};eligible_recipients:string[];refund_policy:string;ledger_visibility:string};balances:{available:number;reserved:number;spent:number;refunded:number;disputed:number};transfers:Array<{id:string;reference:string;source:string;contributor_id:string;requested:number;settled:number;state:string;note?:string}>};
const lines=(x:FormDataEntryValue|null)=>String(x||"").split("\n").map(v=>v.trim()).filter(Boolean);
export function ProjectFunds({repository,actor}:{repository:string;actor:string}){const[items,setItems]=useState<Fund[]>([]),[error,setError]=useState("");const refresh=useCallback(async()=>{const r=await fetch(`/api/repositories/${repository}/funds`);if(r.ok)setItems((await r.json()).items)},[repository]);useEffect(()=>{void refresh()},[refresh]);async function send(path:string,body:unknown){setError("");const r=await fetch(`/api/repositories/${repository}/funds${path}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});if(!r.ok){setError((await r.json()).error);return}await refresh()}
function create(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);void send("",{name:f.get("name"),description:f.get("description"),steward_ids:lines(f.get("stewards")),accepted_funding_sources:lines(f.get("sources")),unit:f.get("unit"),unit_kind:f.get("kind"),spending_limits:{per_allocation:Number(f.get("allocation")),per_recipient:Number(f.get("recipient")),total:Number(f.get("total"))},approval_rule:{minimum_approvals:Number(f.get("approvals")),approver_ids:lines(f.get("approvers")),threshold:Number(f.get("threshold"))},eligible_recipients:lines(f.get("eligible")),refund_policy:f.get("refund"),ledger_visibility:f.get("visibility")})}
return <section className="investigation-workspace"><div className="section-heading"><div><span className="eyebrow">Govern resources before promises</span><h2>Project funds</h2><p>Inspect settled value and the rules for stewardship, approvals, recipients, and refunds. Financial participation grants no repository authority.</p></div><Badge>{items.length} funds</Badge></div>{error&&<p className="form-error">{error}</p>}<details><summary>Create governed fund</summary><form onSubmit={create}><label>Name<input name="name" required/></label><label>Description<textarea name="description"/></label><label>Steward IDs<textarea name="stewards" defaultValue={actor} required/></label><label>Accepted funding sources<textarea name="sources" placeholder="stripe" required/></label><label>Unit<input name="unit" placeholder="USD" required/></label><label>Type<select name="kind"><option>currency</option><option>credit</option></select></label><label>Per-allocation limit<input name="allocation" type="number" min="1" required/></label><label>Per-recipient limit<input name="recipient" type="number" min="1" required/></label><label>Total limit<input name="total" type="number" min="1" required/></label><label>Approver IDs<textarea name="approvers" defaultValue={actor} required/></label><label>Minimum approvals<input name="approvals" type="number" min="1" defaultValue="1" required/></label><label>Approval threshold<input name="threshold" type="number" min="0" defaultValue="0"/></label><label>Eligible recipient classes<textarea name="eligible" defaultValue={"human\napproved_agent_operator"} required/></label><label>Refund policy<textarea name="refund" required/></label><label>Ledger visibility<select name="visibility"><option value="public">Public</option><option value="repository">Repository readers</option></select></label><Button type="submit">Publish fund terms</Button></form></details>{items.map(f=><article className="card" key={f.id}><h3>{f.terms.name} <Badge>{f.terms.unit_kind}</Badge></h3><p>{f.terms.description}</p><p><strong>Balances ({f.terms.unit} minor units):</strong> available {f.balances.available} · reserved {f.balances.reserved} · spent {f.balances.spent} · refunded {f.balances.refunded} · disputed {f.balances.disputed}</p><p><strong>Stewards:</strong> {f.terms.steward_ids.join(" · ")}<br/><strong>Approvals:</strong> {f.terms.approval_rule.minimum_approvals} of {f.terms.approval_rule.approver_ids.join(" · ")} above {f.terms.approval_rule.threshold}<br/><strong>Limits:</strong> allocation {f.terms.spending_limits.per_allocation} · recipient {f.terms.spending_limits.per_recipient} · total {f.terms.spending_limits.total}<br/><strong>Eligible:</strong> {f.terms.eligible_recipients.join(" · ")}<br/><strong>Refunds:</strong> {f.terms.refund_policy}</p><details><summary>Commit resources</summary><form onSubmit={e=>{e.preventDefault();const x=new FormData(e.currentTarget);void send(`/${f.id}/commitments`,{reference:x.get("reference"),source:x.get("source"),amount:Number(x.get("amount")),state:"pending"})}}><label>Provider transfer reference<input name="reference" required/></label><label>Source<select name="source">{f.terms.accepted_funding_sources.map(x=><option key={x}>{x}</option>)}</select></label><label>Amount in minor units<input name="amount" type="number" min="1" required/></label><Button type="submit">Record pending commitment</Button></form></details><h4>Ledger</h4>{f.transfers.length?f.transfers.map(x=><p key={x.id}><Badge>{x.state}</Badge> {x.source} · {x.reference} · requested {x.requested} · settled {x.settled} · contributor {x.contributor_id}{x.note&&` · ${x.note}`}</p>):<p>No commitments recorded.</p>}</article>)}</section>}
